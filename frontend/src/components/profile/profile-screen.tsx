import { RoutePlaceholder } from '@/components/route-placeholder';

export function ProfileScreen() {
  return (
    <RoutePlaceholder
      eyebrow="Profile"
      title="Account and quit profile summary"
      description="Phase 05 will add user data, settings access, and logout handling."
    />
  );
}
